[project]
name = "harmonizer-backend"
version = "0.1.0"
description = "Harmonizer's real alignment/FCPXML/Resolve-integration engine (align.py, make_fcpxml.py, import_to_resolve.py, resolve_validate.py, recompute_segments.py) -- not scratch code, see ../../../migration-plan.md §6.4."
requires-python = ">=3.13"
dependencies = [
    # Unpinned in the original prototype/requirements.txt -- left unpinned
    # here too and resolved consistently against the rest of the shared
    # workspace (numpy/scipy already pinned elsewhere to 2.4.6/1.17.1).
    "numpy",
    "scipy",
    "librosa",
    "soundfile",
]
